services:
  - type: web
    name: backend
    env: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: DB_URL  # Make sure this matches your .env file
        sync: false
      - key: PORT
        value: "5000"
